apiVersion: v1
kind: Service
metadata:
  name: testing-web
  namespace: testing
  labels:
    app.kubernetes.io/name: testing-web
    app.kubernetes.io/part-of: testing
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: testing-web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 32002
